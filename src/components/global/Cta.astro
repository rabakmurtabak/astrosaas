<style>
  /* Parallax shapes */
  .parallax-shapes {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }

  .circle {
    position: absolute;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    animation: float 15s ease-in-out infinite;
    opacity: 0.7;
  }

  .circle-small {
    width: 50px;
    height: 50px;
    top: 30%;
    left: 20%;
    animation-duration: 12s;
  }

  .circle-medium {
    width: 80px;
    height: 80px;
    top: 50%;
    left: 80%;
    animation-duration: 18s;
  }

  .circle-large {
    width: 120px;
    height: 120px;
    top: 80%;
    left: 50%;
    animation-duration: 22s;
  }

  /* Animation for floating effect */
  @keyframes float {
    0% {
      transform: translate(0, 0);
    }
    50% {
      transform: translate(30px, -30px);
    }
    100% {
      transform: translate(0, 0);
    }
  }
</style>

<section class="max-w-7xl mx-auto relative overflow-hidden bg-gradient-to-r from-indigo-600 via-purple-600 to-blue-600 rounded-4xl">
  
  <!-- Parallax Floating Shapes -->
  <div class="parallax-shapes">
    <div class="circle circle-small"></div>
    <div class="circle circle-medium"></div>
    <div class="circle circle-large"></div>
  </div>

  <!-- Content Section -->
  <div class="relative z-10 mx-auto w-full lg:px-24 max-w-7xl md:px-12 items-center px-8 py-48 mt-8">
    <div class="text-center max-w-4xl mx-auto">
      <p class="text-4xl tracking-tight font-semibold text-white md:text-5xl font-display">
        Payments for a Digital World
      </p>
      <p class="text-gray-200 lg:text-xl max-w-2xl mt-4 mx-auto text-base">
        Upgrade the way you collect, convert, and send money using licensed modern and conventional payment systems.
      </p>
    </div>

    <div class="flex justify-center gap-4 mt-10 flex-col sm:flex-row">
      <a
        class="items-center justify-center h-12 font-medium rounded-xl focus-visible:outline-black focus:outline-none inline-flex duration-200 bg-white text-black lg:w-auto px-6 py-3 text-center border-2 border-white hover:bg-transparent hover:border-white hover:text-white transition-all"
        href="https://github.com/michael-andreuzza/astrosaas"
      >
        About Us
      </a>

      <a
        class="items-center justify-center h-12 font-medium rounded-xl focus-visible:outline-black focus:outline-none inline-flex bg-black border-2 border-black duration-200 focus-visible:ring-black hover:bg-transparent hover:border-white hover:text-white lg:w-auto px-6 py-3 text-center text-white transition-all"
        href="https://lexingtonthemes.com"
      >
        Talk to Us &nbsp; &rarr;
      </a>
    </div>
  </div>
</section>
